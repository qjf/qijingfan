// Step 1: upon click of search button, hide the form and show the movie card

// step 2: grab information from form field (only happen when ajax successful)

// step 3: process Ajax request

// step 4 show returned information with handlebars
$(document).ready(function(){
  $(document).on('click',"#movie-search-button",function(event){ //parameter
    alert('work');
  });
});
